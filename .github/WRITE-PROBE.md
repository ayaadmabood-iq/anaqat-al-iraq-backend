# write probe

If you see this file, MCP's create_or_update_file succeeded — the MCP
GitHub App has broader write permissions than the git-proxy path this
session uses for `git push`. This file will be removed immediately.
